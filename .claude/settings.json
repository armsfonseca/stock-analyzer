{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/c/Users/armsf/OneDrive/Documentos/Claude_Angelo/stock-analyzer\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: update via Claude Code\" && git push)",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
